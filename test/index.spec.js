import store from '../src/store/index';
import { ADD, DONE, DELETE } from '../src/store/mutation-types';
const dispatch = store.dispatch;
const commit = store.commit;
const getters = store.getters;
describe(actions, () => {
  it('ADD', () => {});
  it('DONE', () => {});
  it('DELETE', () => {});
});
describe(mutations, () => {
  it('ADD', () => {});
  it('DONE', () => {});
  it('DELETE', () => {});
});
describe(getters, () => {});